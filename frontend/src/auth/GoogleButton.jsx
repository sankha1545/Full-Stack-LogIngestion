export default function GoogleButton() {
  const handleGoogle = async () => {
    // get Google token → POST /api/auth/google
  };

  return <button onClick={handleGoogle}>Sign in with Google</button>;
}
